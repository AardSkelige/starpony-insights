/**
 * Обойма для иконок: рамка вокруг группы, кнопки внутри без своих.
 * Так соседние группы — действия и тема — читаются как один ряд, а не как
 * россыпь одинаковых кнопок (макет, шапка приложения).
 *
 * Отдельным файлом, а не рядом с шапкой: её использует и переключатель темы,
 * который сама шапка и подключает. Импорт друг в друга работал бы, но кольцо
 * в зависимостях — то, что ломается позже и не там, где написано.
 */
export const ICON_GROUP =
  "flex items-center gap-0.5 rounded-lg border border-input bg-background p-0.5"
