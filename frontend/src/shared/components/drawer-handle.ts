/**
 * Полоска над выдвижной панелью — та, за которую её тянут вниз.
 *
 * Рисуется псевдоэлементом на самой панели, а не её содержимым: `showSwipeHandle`
 * из реестра ставит полоску **внутри**, на белом фоне, где она читается как
 * часть карточки. Снаружи, на затемнении, она сразу говорит, что панель
 * двигается пальцем.
 *
 * Псевдоэлемент не обрезается: `overflow-hidden` стоит на внутреннем блоке
 * панели, а не на ней самой.
 *
 * Цвет — `foreground` с прозрачностью: он инвертируется вместе с темой,
 * поэтому полоска видна и на светлом затемнении, и на тёмном.
 */
export const DRAWER_HANDLE =
  "before:absolute before:-top-3 before:left-1/2 before:h-1.5 before:w-10 " +
  "before:-translate-x-1/2 before:rounded-full before:bg-foreground/30 before:content-['']"

/**
 * Высота панели на телефоне фиксирована.
 *
 * Иначе она подстраивается под содержимое: приехал скелетон — одна высота,
 * приехали данные — другая, переключили вкладку — третья. Каждая смена
 * анимируется, и панель дёргается на глазах, будто что-то подгружает.
 */
export const DRAWER_PHONE_HEIGHT = "max-sm:h-[85svh]"
