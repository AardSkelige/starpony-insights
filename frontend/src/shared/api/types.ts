/**
 * Типы, общие для нескольких разделов.
 *
 * Берутся из схемы OpenAPI, а не пишутся руками: схема генерируется из
 * сериализаторов Django, и расхождение фронта с бэком ловится на сборке.
 * Здесь только псевдонимы — чтобы `shared/`-компоненты не импортировали
 * тип из раздела, которому он случайно первым понадобился.
 */

import type { components } from "@/shared/api/schema"

/** Канал продаж в выпадающем списке фильтров. */
export type SalesChannel = components["schemas"]["SalesChannel"]

/** Остаток на складе. Один тип на все разделы — так же, как на сервере. */
export type Stock = components["schemas"]["Stock"]
