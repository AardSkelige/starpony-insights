"""Обратная запись в учёт: единственное место, где мы меняем чужие данные.

Правила — `CLAUDE.md` §6: журнал, выключатель, `--dry-run`. Механика журнала
общая и живёт в `journal.py`; каждый вид записи — свой модуль рядом.
"""
