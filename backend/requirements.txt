# Ядро
Django==5.2.17
djangorestframework==3.18.0
psycopg[binary]==3.3.4
python-dotenv==1.2.3

# Контракт API — схема, из которой генерируются типы фронтенда
drf-spectacular==0.30.0
# Статика страницы /api/docs/ — локально, а не с внешнего CDN: интерактивная
# документация не должна зависеть от чужой сети и от версии, которая меняется
# сама. 2.5 МБ против 337 МБ образа.
drf-spectacular-sidecar==2026.8.1

# МойСклад
requests==2.34.2

# Выгрузка таблиц в Excel. openpyxl, а не pandas: нужен только writer,
# а pandas тянет numpy — сто мегабайт в образ ради того, что делается
# двумя циклами.
openpyxl==3.1.5

# Прод
gunicorn==26.2.0

# Разработка и тесты
pytest==9.0.2
pytest-django==4.14.0
responses==0.26.3
